<UserControl x:Class="FFXI_LogAnalyzer.App.UnparsedLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="320"
             d:DesignWidth="900">
    <DataGrid ItemsSource="{Binding UnparsedLogs}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column">
        <DataGrid.Resources>
            <Style x:Key="WrappingTextBlockStyle" TargetType="{x:Type TextBlock}">
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="VerticalAlignment" Value="Top" />
            </Style>
        </DataGrid.Resources>
        <DataGrid.Columns>
            <DataGridTextColumn Header="action_group_key"
                                Binding="{Binding ActionGroupKey}"
                                Width="220" />
            <DataGridTextColumn Header="order範囲"
                                Binding="{Binding OrderRange}"
                                Width="90" />
            <DataGridTextColumn Header="event_group"
                                Binding="{Binding EventGroup}"
                                Width="130" />
            <DataGridTextColumn Header="visible_text一覧"
                                Binding="{Binding VisibleTexts}"
                                ElementStyle="{StaticResource WrappingTextBlockStyle}"
                                Width="*" />
            <DataGridTextColumn Header="解析不能理由"
                                Binding="{Binding Reason}"
                                ElementStyle="{StaticResource WrappingTextBlockStyle}"
                                Width="220" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
